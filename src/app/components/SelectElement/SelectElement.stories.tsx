import SelectElement from './SelectElement';

export default {
  component: SelectElement,
  title: 'Components/SelectElement',
  parameters: {
    layout: 'centered',
  },
};

export const Default = (): JSX.Element => <SelectElement />;
